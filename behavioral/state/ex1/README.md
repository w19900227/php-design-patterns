

### Request 
`/state/ex1`

### Response
	Now state: OffState
	Lights on!
	After state : OnState
	-----------------------------------------
	Now state: OnState
	Light is already on -> take no action
	After state : OnState
	-----------------------------------------
	Now state : OnState
	Lights off!
	After state : OffState
	-----------------------------------------
	Now state : OffState
	Light is already off -> take no action
	After state : OffState

	
